import React from 'react';
import classes from './ChatHeader.module.scss';

const ChatHeader = () => {
  return (
    <div className={classes.header}>
      <div className={classes.header__container}>header</div>
    </div>
  );
};

export default ChatHeader;
